# `src/content` Agent Guide

This directory is the single source of truth for apps, translations, and localized paths.

- Keep every supported locale complete and type checked.
- Add an app through the shared catalog contract; do not create parallel data shapes.
- Keep route slugs, navigation labels, metadata, and public copy centralized.
- Match claims to the relevant product brief and specs.
- Do not import React or UI code here.

