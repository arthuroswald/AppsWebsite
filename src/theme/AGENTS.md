# `src/theme` Agent Guide

Theme modules define design tokens only.

- Keep shared colors, spacing, typography, radius, shadows, and motion values here.
- Keep app-specific tokens isolated so one app's identity does not dominate the site.
- Do not add components, localized content, or routing logic.
- Prefer named CSS custom properties over repeated magic values.

